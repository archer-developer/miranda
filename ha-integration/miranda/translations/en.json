{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Miranda",
        "description": "Enter the address of your running Miranda Agent Service.",
        "data": {
          "base_url": "Base URL (e.g. http://192.168.1.50:8787)",
          "auth_token": "Bearer token — the value of MIRANDA_AUTH_TOKEN on the server (leave empty if none is set)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach Miranda at that address. Check the URL, that Miranda is running, and that this Home Assistant instance can reach it on the network."
    },
    "abort": {
      "already_configured": "This Miranda instance is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Miranda options",
        "description": "The token can be added or changed here at any time — it does not require re-adding the integration.",
        "data": {
          "timeout": "Request timeout (seconds)",
          "auth_token": "Bearer token — the value of MIRANDA_AUTH_TOKEN on the server (leave empty if none is set)"
        }
      }
    }
  }
}
